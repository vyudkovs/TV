'use strict';


/**
 * adds environment
 * Adds environment
 *
 * environmentItem EnvironmentItem Environment item to add (optional)
 * no response value expected for this operation
 **/
exports.addEnvironment = function(environmentItem) {
  return new Promise(function(resolve, reject) {
    resolve();
  });
}

